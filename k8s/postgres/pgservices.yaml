apiVersion: v1
kind: Service
metadata:
  name: fiap-postgres
  namespace: fiap
  labels:
    app: fiap-postgres
spec:
  clusterIP: "None"
  selector:
    app: fiap-postgres
  ports:
    - port: 5432
      name: postgres