apiVersion: v1
kind: Service
metadata:
  labels:
    app: ext-lb
  name: ext-lb
  namespace: fiap
spec:
  ports:
  - name: https
    port: 443
    protocol: TCP
    targetPort: 8080
  selector:
    app: oficina
  type: LoadBalancer