apiVersion: v1
kind: Service
metadata:
  labels:
    app: ext-lb
  name: ext-lb
  namespace: fiap
spec:
  ports:
  - name: 3000-8080
    port: 3000
    protocol: TCP
    targetPort: 8080
  selector:
    app: oficina
  type: LoadBalancer