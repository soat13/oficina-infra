openapi: "3.0.3"
info:
  title: Oficina API
  version: 1.0.0
  description: |-
    API da Oficina.

    Utilize `POST /auth/login` para obter um token JWT e, em seguida, informe `Bearer <token>` no cabeçalho `Authorization` ao chamar as rotas protegidas. No Swagger UI, use o botão **Authorize** para registrar o token.
servers:
  - url: /
    description: Servidor atual
security:
  - bearerAuth: []
tags:
  - name: Auth
    description: Fluxo de autenticação e emissão de tokens JWT.
  - name: Users
    description: Administração de usuários internos.
  - name: Customers
    description: Cadastro de clientes.
  - name: Vehicles
    description: Cadastro de veículos vinculados a clientes.
  - name: Services
    description: Catálogo de serviços oferecidos.
  - name: Products
    description: Catálogo de produtos e controle de estoque.
  - name: Repair Orders
    description: Gerenciamento das ordens de reparo.
  - name: Estimates
    description: Orçamentos gerados a partir de ordens de reparo.
  - name: Webhooks
    description: Recebimento de notificações de serviços externos.
paths:
  /auth/login:
    post:
      tags: [ Auth ]
      summary: Autenticar usuário
      description: Retorna um token JWT válido para acessar as rotas protegidas.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/LoginRequest"
            example:
              email: admin@example.com
              password: password123
      responses:
        "200":
          description: Autenticação realizada com sucesso.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/LoginResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          description: Credenciais inválidas.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "401"
                message: invalid credentials
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/auth/login"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /webhooks/mercado-pago:
    post:
      tags: [ Webhooks ]
      summary: Webhook Mercado Pago
      description: Recebe notificações de eventos do Mercado Pago.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
      responses:
        "200":
          description: Notificação recebida com sucesso.
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/webhook/mercado-pago"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/services:
    get:
      tags: [ Services ]
      summary: Listar serviços
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de serviços.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ServiceListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/services"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Services ]
      summary: Criar serviço
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceCreateRequest"
      responses:
        "201":
          description: Serviço criado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "409":
          description: Serviço duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: service with this name already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/services"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/services/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Services ]
      summary: Obter serviço por ID
      responses:
        "200":
          description: Serviço encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Service"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Serviço não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: service not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/services/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    patch:
      tags: [ Services ]
      summary: Atualizar serviço
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ServiceUpdateRequest"
      responses:
        "204":
          description: Serviço atualizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Serviço não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: service not found
        "409":
          description: Serviço duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: service with this name already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/services/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "PATCH"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    delete:
      tags: [ Services ]
      summary: Remover serviço
      responses:
        "204":
          description: Serviço removido.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Serviço não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: service not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/services/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/products:
    get:
      tags: [ Products ]
      summary: Listar produtos
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de produtos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ProductListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/products"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Products ]
      summary: Criar produto
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductCreateRequest"
      responses:
        "201":
          description: Produto criado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "409":
          description: Produto duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: product with this name already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/products"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/products/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Products ]
      summary: Obter produto por ID
      responses:
        "200":
          description: Produto encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Product"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Produto não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: product not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/products/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    patch:
      tags: [ Products ]
      summary: Atualizar produto
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ProductUpdateRequest"
      responses:
        "204":
          description: Produto atualizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Produto não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: product not found
        "409":
          description: Produto duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: product with this name already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/products/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "PATCH"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    delete:
      tags: [ Products ]
      summary: Remover produto
      responses:
        "204":
          description: Produto removido.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Produto não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: product not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/products/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/customers:
    get:
      tags: [ Customers ]
      summary: Listar clientes
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de clientes.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/CustomerListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Customers ]
      summary: Criar cliente
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerCreateRequest"
      responses:
        "201":
          description: Cliente criado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "409":
          description: Cliente duplicado (documento ou email já existente).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                duplicateDocument:
                  value:
                    code: "409"
                    message: customer with this document already exists
                duplicateEmail:
                  value:
                    code: "409"
                    message: customer with this email already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/customers/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Customers ]
      summary: Obter cliente por ID
      responses:
        "200":
          description: Cliente encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Customer"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Cliente não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: customer not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    patch:
      tags: [ Customers ]
      summary: Atualizar cliente
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/CustomerUpdateRequest"
      responses:
        "204":
          description: Cliente atualizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Cliente não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: customer not found
        "409":
          description: Email duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: customer with this email already exists
                message: "409"
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "PATCH"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    delete:
      tags: [ Customers ]
      summary: Remover cliente
      responses:
        "204":
          description: Cliente removido.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Cliente não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: customer not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/customers/{id}/vehicles:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Vehicles ]
      summary: Listar veículos por cliente
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Veículos do cliente.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleListResponse"
        "400":
          description: ID de cliente inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/customers/{id}/vehicles"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/vehicles:
    get:
      tags: [ Vehicles ]
      summary: Listar veículos
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de veículos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VehicleListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/vehicles"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Vehicles ]
      summary: Criar veículo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VehicleCreateRequest"
      responses:
        "201":
          description: Veículo criado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "409":
          description: Placa duplicada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: vehicle plate already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/vehicles"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/vehicles/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Vehicles ]
      summary: Obter veículo por ID
      responses:
        "200":
          description: Veículo encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Vehicle"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Veículo não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: vehicle not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/vehicles/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    patch:
      tags: [ Vehicles ]
      summary: Atualizar veículo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VehicleUpdateRequest"
      responses:
        "204":
          description: Veículo atualizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Veículo não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: vehicle not found
        "409":
          description: Placa duplicada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: vehicle plate already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/vehicles/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "PATCH"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    delete:
      tags: [ Vehicles ]
      summary: Remover veículo
      responses:
        "204":
          description: Veículo removido.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Veículo não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: vehicle not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/vehicles/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/users:
    get:
      tags: [ Users ]
      summary: Listar usuários
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de usuários.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/UserListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/users"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Users ]
      summary: Criar usuário
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserCreateRequest"
      responses:
        "201":
          description: Usuário criado.
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "409":
          description: Usuário duplicado (documento ou email já existente).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                duplicateDocument:
                  value:
                    code: "409"
                    message: user with this document already exists
                duplicateEmail:
                  value:
                    code: "409"
                    message: user with this email already exists
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/users"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/users/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Users ]
      summary: Obter usuário por ID
      responses:
        "200":
          description: Usuário encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/User"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Usuário não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: user not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/users/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    patch:
      tags: [ Users ]
      summary: Atualizar usuário
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/UserUpdateRequest"
      responses:
        "204":
          description: Usuário atualizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Usuário não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: user not found
        "409":
          description: Email duplicado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: user with this email already exists
                message: "409"
        "422":
          $ref: "#/components/responses/UnprocessableEntityResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/users/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "PATCH"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
    delete:
      tags: [ Users ]
      summary: Remover usuário
      responses:
        "204":
          description: Usuário removido.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Usuário não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: user not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/users/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders:
    get:
      tags: [ Repair Orders ]
      summary: Listar ordens de reparo
      parameters:
        - $ref: "#/components/parameters/LimitParam"
        - $ref: "#/components/parameters/OffsetParam"
      responses:
        "200":
          description: Lista de ordens de reparo.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RepairOrderListResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
    post:
      tags: [ Repair Orders ]
      summary: Criar ordem de reparo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/RepairOrderCreateRequest"
      responses:
        "201":
          description: Ordem de reparo criada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/IdResponse"
        "400":
          description: Corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "422":
          description: Veículo ou cliente não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ValidationError"
              example:
                code: "422"
                message: vehicle or customer not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/average-execution-time:
    get:
      tags: [ Repair Orders ]
      summary: Obter tempo médio de execução
      responses:
        "200":
          description: Tempo médio em minutos (pode ser nulo se inexistente).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/AverageExecutionTimeResponse"
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/average-execution-time"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    get:
      tags: [ Repair Orders ]
      summary: Obter ordem de reparo por ID
      responses:
        "200":
          description: Ordem de reparo encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/RepairOrder"
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "GET"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/cancel:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Cancelar ordem de reparo
      responses:
        "204":
          description: Ordem cancelada.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/cancel"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/start-diagnostics:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Iniciar diagnóstico da ordem
      responses:
        "204":
          description: Diagnóstico iniciado.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/start-diagnostics"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/finish-diagnostics:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Finalizar diagnóstico da ordem
      description: Finaliza o diagnóstico informando produtos e serviços necessários
        para o reparo.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/FinishDiagnosticsRequest"
            example:
              products:
                - id: "3fa85f64-5717-4562-b3fc-2c963f66afa6"
                  quantity: 2
                - id: "4fb85f64-5717-4562-b3fc-2c963f66afa7"
                  quantity: 1
              services:
                - id: "5fc85f64-5717-4562-b3fc-2c963f66afa8"
                  quantity: 1
                - id: "6fd85f64-5717-4562-b3fc-2c963f66afa9"
                  quantity: 1
      responses:
        "204":
          description: Diagnóstico finalizado.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                repairOrderNotFound:
                  value:
                    code: "404"
                    message: repair order not found
        "422":
          description: Validação falhou - erros de negócio ou validação.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                productNotFound:
                  value:
                    code: "422"
                    message: product not found
                serviceNotFound:
                  value:
                    code: "422"
                    message: service not found
                noProductsOrServices:
                  value:
                    code: "422"
                    message: at least one product and service is required
                quantityInvalid:
                  value:
                    code: "422"
                    message: product or service quantity must be greater than zero
                insufficientStock:
                  value:
                    code: "422"
                    message: insufficient stock for one or more products
                validationError:
                  value:
                    code: INVALID_BODY
                    message: Body validation failed
                    errors:
                      - field: products
                        message: products is required
                      - field: services
                        message: services is required
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/finish-diagnostics"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/start-execution:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Iniciar execução da ordem
      responses:
        "204":
          description: Execução iniciada.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/start-execution"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/finish-execution:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Finalizar execução da ordem
      responses:
        "204":
          description: Execução finalizada.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/finish-execution"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/release-vehicle:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Repair Orders ]
      summary: Liberar veículo
      responses:
        "204":
          description: Veículo liberado.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Ordem de reparo não encontrada.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: repair order not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/release-vehicle"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/estimate/approve:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Estimates ]
      summary: Aprovar orçamento
      responses:
        "200":
          description: Orçamento aprovado.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Orçamento não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: estimate not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/estimate/approve"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/repair-orders/{id}/estimate/reject:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Estimates ]
      summary: Rejeitar orçamento
      responses:
        "200":
          description: Orçamento rejeitado.
        "400":
          description: ID inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Orçamento não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: estimate not found
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/repair-orders/{id}/estimate/reject"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/estimates/{id}/items:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
    post:
      tags: [ Estimates ]
      summary: Adicionar item ao orçamento
      description: Adiciona um produto ou serviço ao orçamento existente.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/AddItemRequest"
      responses:
        "200":
          description: Item adicionado com sucesso.
        "400":
          description: ID inválido ou corpo JSON inválido.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                invalidId:
                  value:
                    code: "400"
                    message: invalid ID
                invalidJson:
                  value:
                    code: "400"
                    message: invalid JSON body
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Orçamento não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: estimate not found
        "409":
          description: Produto indisponível ou não é possível alterar itens após aprovação.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                productNotAvailable:
                  value:
                    code: "409"
                    message: product not available
                cannotChangeItems:
                  value:
                    code: "409"
                    message: cannot change items after approval
        "422":
          description: Validação de negócio falhou (produto/serviço não encontrado ou
            quantidade inválida).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                productOrServiceNotFound:
                  value:
                    code: "422"
                    message: product or service not found
                quantityInvalid:
                  value:
                    code: "422"
                    message: quantity must be greater than zero
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/estimates/{id}/items"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "POST"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
  /admin/estimates/{id}/items/{itemId}:
    parameters:
      - $ref: "#/components/parameters/ResourceIdParam"
      - name: itemId
        in: path
        required: true
        description: ID do item (produto ou serviço) a ser removido.
        schema:
          type: string
          format: uuid
    delete:
      tags: [ Estimates ]
      summary: Remover item do orçamento
      responses:
        "200":
          description: Item removido com sucesso.
        "400":
          description: IDs inválidos.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "400"
                message: invalid ID
        "401":
          $ref: "#/components/responses/UnauthorizedResponse"
        "404":
          description: Orçamento não encontrado.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "404"
                message: estimate not found
        "409":
          description: Não é possível alterar itens após aprovação.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              example:
                code: "409"
                message: cannot change items after approval
        "422":
          description: Validação de negócio falhou (item não encontrado ou orçamento
            precisa ter pelo menos um item).
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/Error"
              examples:
                itemNotFound:
                  value:
                    code: "422"
                    message: item not found in estimate
                mustHaveAtLeastOneItem:
                  value:
                    code: "422"
                    message: estimate must have at least one item
      x-amazon-apigateway-integration:
        uri: "${load_balancer_uri}/admin/estimates/{id}/items/{itemId}"
        responses:
          default:
            statusCode: "200"
        passthroughBehavior: "when_no_match"
        httpMethod: "DELETE"
        type: "http_proxy"
        requestParameters:
          integration.request.path.id: "method.request.path.id"
          integration.request.header.X-Service-Token: "'${service_token}'"
          integration.request.path.itemId: "method.request.path.itemId"
components:
  securitySchemes:
    bearerAuth:
      type: apiKey
      name: Authorization
      in: header
      x-amazon-apigateway-authtype: custom
      x-amazon-apigateway-authorizer:
        type: token
        authorizerUri: "${auth_lambda_invoke_arn}"
        authorizerResultTtlInSeconds: 300
  parameters:
    LimitParam:
      name: limit
      in: query
      description: Número máximo de registros a retornar.
      schema:
        type: integer
        minimum: 1
        default: 50
    OffsetParam:
      name: offset
      in: query
      description: Quantidade de registros a ignorar antes de retornar resultados.
      schema:
        type: integer
        minimum: 0
        default: 0
    ResourceIdParam:
      name: id
      in: path
      required: true
      description: Identificador do recurso.
      schema:
        type: string
        format: uuid
    CustomerIdParam:
      name: customerId
      in: path
      required: true
      description: Identificador do cliente.
      schema:
        type: string
        format: uuid
  responses:
    UnauthorizedResponse:
      description: Token ausente, inválido ou expirado.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
          examples:
            missingToken:
              value:
                code: "401"
                message: missing authorization token
            invalidToken:
              value:
                code: "401"
                message: invalid token
            expiredToken:
              value:
                code: "401"
                message: token expired
    InvalidJSONResponse:
      description: Corpo JSON inválido.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    UnprocessableEntityResponse:
      description: Entidade não processável (erros de validação ou regras de negócio).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/ValidationError"
    InvalidIDResponse:
      description: Identificador inválido.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    NotFoundResponse:
      description: Recurso não encontrado.
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
    ConflictResponse:
      description: Conflito (recurso duplicado ou operação inválida).
      content:
        application/json:
          schema:
            $ref: "#/components/schemas/Error"
  schemas:
    Error:
      type: object
      required: [ code, message ]
      properties:
        code:
          type: string
          example: INVALID_JSON
        message:
          type: string
          example: invalid JSON body
    IdResponse:
      type: object
      required: [ id ]
      properties:
        id:
          type: string
          format: uuid
          description: ID do recurso criado.
    ValidationError:
      type: object
      required: [ code, message ]
      properties:
        code:
          type: string
          example: INVALID_BODY
        message:
          type: string
          example: Body validation failed
        errors:
          type: array
          items:
            type: object
            properties:
              field:
                type: string
                example: password
              message:
                type: string
                example: Password is a required field
    LoginRequest:
      type: object
      required: [ email, password ]
      properties:
        email:
          type: string
          format: email
        password:
          type: string
          minLength: 8
    AuthenticatedUser:
      type: object
      required: [ id, name, email, roles ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        email:
          type: string
          format: email
        roles:
          type: array
          items:
            type: string
    LoginResponse:
      type: object
      required: [ access_token, token_type, expires_in, expires_at, user ]
      properties:
        access_token:
          type: string
        token_type:
          type: string
          example: Bearer
        expires_in:
          type: integer
          format: int64
          description: Tempo (em segundos) até a expiração do token.
        expires_at:
          type: string
          format: date-time
        user:
          $ref: "#/components/schemas/AuthenticatedUser"
    Service:
      type: object
      required: [ id, name, price ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        price:
          type: integer
          format: int64
          description: Preço do serviço em centavos (BRL).
    ServiceCreateRequest:
      type: object
      required: [ name, price ]
      properties:
        name:
          type: string
          minLength: 3
        price:
          type: integer
          format: int64
          minimum: 1
          description: Preço do serviço em centavos (BRL).
    ServiceUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 3
        price:
          type: integer
          format: int64
          minimum: 1
          description: Preço do serviço em centavos (BRL).
    ServiceListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Service"
    Product:
      type: object
      required: [ id, name, price, stock ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        price:
          type: integer
          format: int64
          description: Preço do produto em centavos (BRL).
        stock:
          type: integer
          minimum: 0
    ProductCreateRequest:
      type: object
      required: [ name, price ]
      properties:
        name:
          type: string
          minLength: 3
        price:
          type: integer
          format: int64
          minimum: 1
          description: Preço do produto em centavos (BRL).
        stock:
          type: integer
          minimum: 0
          default: 0
    ProductUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 3
        price:
          type: integer
          format: int64
          minimum: 1
          description: Preço do produto em centavos (BRL).
        stock:
          type: integer
          minimum: 0
    ProductListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Product"
    Customer:
      type: object
      required: [ id, name, document, document_type, email, phone_number ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        document:
          type: string
        document_type:
          type: string
          description: Tipo de documento (CPF ou CNPJ).
        email:
          type: string
          format: email
        phone_number:
          type: string
    CustomerCreateRequest:
      type: object
      required: [ name, document, email, phone_number ]
      properties:
        name:
          type: string
          minLength: 3
        document:
          type: string
          description: CPF ou CNPJ sem pontuação.
        email:
          type: string
          format: email
        phone_number:
          type: string
          description: Telefone no formato E.164 ou somente dígitos (mínimo 11).
    CustomerUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 3
        email:
          type: string
          format: email
        phone_number:
          type: string
          description: Telefone atualizado.
    CustomerListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Customer"
    Vehicle:
      type: object
      required: [ id, customer_id, plate, brand, model, year ]
      properties:
        id:
          type: string
          format: uuid
        customer_id:
          type: string
          format: uuid
        plate:
          type: string
        brand:
          type: string
        model:
          type: string
        year:
          type: integer
    VehicleCreateRequest:
      type: object
      required: [ customer_id, plate, brand, model, year ]
      properties:
        customer_id:
          type: string
          format: uuid
        plate:
          type: string
          description: Placa do veículo.
        brand:
          type: string
        model:
          type: string
        year:
          type: integer
          minimum: 1900
    VehicleUpdateRequest:
      type: object
      properties:
        plate:
          type: string
        brand:
          type: string
        model:
          type: string
        year:
          type: integer
          minimum: 1900
    VehicleListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/Vehicle"
    User:
      type: object
      required: [ id, name, document, document_type, email, phone_number, roles ]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        document:
          type: string
        document_type:
          type: string
        email:
          type: string
          format: email
        phone_number:
          type: string
        roles:
          type: array
          items:
            type: string
    UserCreateRequest:
      type: object
      required: [ name, document, email, phone_number, password, roles ]
      properties:
        name:
          type: string
          minLength: 3
        document:
          type: string
        email:
          type: string
          format: email
        phone_number:
          type: string
        password:
          type: string
          minLength: 8
        roles:
          type: array
          minItems: 1
          items:
            type: string
    UserUpdateRequest:
      type: object
      properties:
        name:
          type: string
          minLength: 3
        email:
          type: string
          format: email
        phone_number:
          type: string
        password:
          type: string
          minLength: 8
        roles:
          type: array
          items:
            type: string
    UserListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/User"
    RepairOrder:
      type: object
      required: [ id, customer_id, vehicle_id, status, created_at, updated_at ]
      properties:
        id:
          type: string
          format: uuid
        customer_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
        status:
          type: string
          description: Status atual da ordem (por exemplo, created, in_execution,
            finished, etc).
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
    RepairOrderCreateRequest:
      type: object
      required: [ customer_id, vehicle_id ]
      properties:
        customer_id:
          type: string
          format: uuid
        vehicle_id:
          type: string
          format: uuid
    RepairOrderListResponse:
      type: object
      required: [ data ]
      properties:
        data:
          type: array
          items:
            $ref: "#/components/schemas/RepairOrder"
    AverageExecutionTimeResponse:
      type: object
      properties:
        AverageMinutes:
          type: number
          format: float
          nullable: true
          description: Tempo médio de execução das ordens em minutos.
    DiagnosticProductItem:
      type: object
      required: [ id, quantity ]
      properties:
        id:
          type: string
          format: uuid
          description: ID do produto.
        quantity:
          type: integer
          minimum: 1
          description: Quantidade necessária do produto.
    DiagnosticServiceItem:
      type: object
      required: [ id, quantity ]
      properties:
        id:
          type: string
          format: uuid
          description: ID do serviço.
        quantity:
          type: integer
          minimum: 1
          description: Quantidade do serviço.
    FinishDiagnosticsRequest:
      type: object
      required: [ products, services ]
      properties:
        products:
          type: array
          items:
            $ref: "#/components/schemas/DiagnosticProductItem"
          minItems: 1
          description: Lista de produtos necessários para o reparo.
        services:
          type: array
          items:
            $ref: "#/components/schemas/DiagnosticServiceItem"
          minItems: 1
          description: Lista de serviços necessários para o reparo.
      description: É necessário informar ao menos um produto e um serviço para
        finalizar o diagnóstico.
    AddItemRequest:
      type: object
      required: [ item_id, quantity ]
      properties:
        item_id:
          type: string
          format: uuid
          description: ID do produto ou serviço a ser adicionado.
        quantity:
          type: integer
          minimum: 1
          description: Quantidade a ser adicionada.
